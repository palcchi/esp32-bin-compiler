const $=s=>document.querySelector(s),c=$('#out'),x=c.getContext('2d');let img=null;function render(){x.fillStyle='#000';x.fillRect(0,0,128,64);if(!img)return;const cover=$('#fit').value==='cover',r=cover?Math.max(128/img.width,64/img.height):Math.min(128/img.width,64/img.height),w=img.width*r,h=img.height*r;x.drawImage(img,(128-w)/2,(64-h)/2,w,h);const d=x.getImageData(0,0,128,64),t=+$(' #thr'.trim()).value,inv=$('#invert').checked,dit=$('#dither').checked;for(let i=0;i<d.data.length;i+=4){let lum=.299*d.data[i]+.587*d.data[i+1]+.114*d.data[i+2];if(dit){const p=(i/4),xx=p%128,yy=Math.floor(p/128);lum+=((xx+yy)%2?18:-18)}let on=lum>t;if(inv)on=!on;d.data[i]=d.data[i+1]=d.data[i+2]=on?255:0}x.putImageData(d,0,0)}$('#file').onchange=e=>{const f=e.target.files[0];if(!f)return;const im=new Image;im.onload=()=>{img=im;render()};im.src=URL.createObjectURL(f)};['thr','fit','dither','invert'].forEach(id=>$('#'+id).oninput=render);$('#export').onclick=()=>{const d=x.getImageData(0,0,128,64).data,b=[];for(let y=0;y<64;y++)for(let bx=0;bx<16;bx++){let v=0;for(let bit=0;bit<8;bit++){const xx=bx*8+bit,i=(y*128+xx)*4;if(d[i]>127)v|=1<<(7-bit)}b.push(v)}const s='#pragma once\nconst unsigned char oledImage[1024] PROGMEM={'+b.join(',')+'};\n';const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([s]));a.download='OLEDImage.h';a.click()};render();