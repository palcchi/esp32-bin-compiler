const $=s=>document.querySelector(s),c=$('#lyricOled'),ctx=c.getContext('2d'),ta=$('#lyrics'),style=$('#style'),animation=$('#animation'),size=$('#size'),inv=$('#invertLyric'),status=$('#lyricStatus'),timeStatus=$('#timeStatus'),list=$('#parsedScenes'),count=$('#sceneCount');let raf=0,idx=0;
function parse(){return ta.value.split('\n').map((raw,lineIndex)=>({raw,lineIndex})).filter(v=>v.raw.trim()).map(({raw,lineIndex})=>{let parts=raw.split('|').map(v=>v.trim()),invert=false;if(parts.length&&/^(inv|invert)$/i.test(parts[parts.length-1])){invert=true;parts.pop()}let seconds=1.5;if(parts.length>1){const maybe=parseFloat(parts[parts.length-1]);if(Number.isFinite(maybe)){seconds=Math.max(.05,maybe);parts.pop()}}let sourceText=parts.join(' | ').trim(),text=/^\[pause\]$/i.test(sourceText)?'':sourceText;return{text,sourceText,seconds,invert,lineIndex}})}
function wrapWords(text,maxWidth){const words=text.trim().split(/\s+/).filter(Boolean),lines=[];let line='';for(const word of words){const test=line?line+' '+word:word;if(line&&ctx.measureText(test).width>maxWidth){lines.push(line);line=word}else line=test}if(line)lines.push(line);return lines}
function drawWrapped(text,fontFamily,fontWeight='400',condense=1,maxLines=4,transform={}){const sc=+size.value;let fontSize=style.value==='brat'?[16,20,24][sc-1]:style.value==='tiny'?[8,10,12][sc-1]:[11,14,17][sc-1],lines=[];const virtualWidth=122/condense;while(fontSize>=7){ctx.font=`${fontWeight} ${fontSize}px ${fontFamily}`;lines=wrapWords(text,virtualWidth);const lineH=fontSize*(style.value==='brat'?.88:1.05);if(lines.length<=maxLines&&lines.length*lineH<=58)break;fontSize--}const lineH=fontSize*(style.value==='brat'?.88:1.05),startY=-(lines.length-1)*lineH/2;ctx.save();ctx.translate(64+(transform.x||0),32+(transform.y||0));ctx.scale(condense*(transform.scale||1),(style.value==='brat'?1.06:1)*(transform.scale||1));ctx.font=`${fontWeight} ${fontSize}px ${fontFamily}`;ctx.textAlign='center';ctx.textBaseline='middle';lines.slice(0,maxLines).forEach((line,i)=>ctx.fillText(line,0,startY+i*lineH,virtualWidth));ctx.restore()}
function easeOutBack(t){const c1=1.70158,c3=c1+1;return 1+c3*Math.pow(t-1,3)+c1*Math.pow(t-1,2)}
function motion(progress){const a=animation.value,p=Math.max(0,Math.min(1,progress));if(a==='pop'){const q=easeOutBack(Math.min(1,p/.35));return{scale:.35+.65*q}}if(a==='slide'){const q=1-Math.pow(1-Math.min(1,p/.3),3);return{y:22*(1-q)}}if(a==='shake'){if(p>.35)return{};const amp=5*(1-p/.35);return{x:Math.sin(p*80)*amp,y:Math.cos(p*55)*amp*.35}}return{}}
function draw(text='',invert=false,progress=1){ctx.fillStyle=invert?'#e9fdff':'#000';ctx.fillRect(0,0,128,64);ctx.fillStyle=invert?'#001116':'#e9fdff';if(!text)return;const tr=motion(progress);if(style.value==='brat')drawWrapped(text.toLowerCase(),'Arial Narrow,Arial,sans-serif','400',.78,4,tr);else if(style.value==='tiny')drawWrapped(text,'ui-monospace,monospace','400',1,5,tr);else drawWrapped(text,'Arial,sans-serif','600',1,4,tr)}
function syncSelected(){const scenes=parse(),s=scenes[idx];inv.checked=!!s?.invert}
function selectScene(i){stop();const scenes=parse();if(!scenes.length)return;idx=Math.max(0,Math.min(i,scenes.length-1));const s=scenes[idx];draw(s.text,s.invert,1);status.textContent=`Scene ${idx+1} · ${s.text||'Pause'}${s.invert?' · INVERT':''}`;timeStatus.textContent=`${s.seconds.toFixed(2)}s`;syncSelected();renderList()}
function setSceneInvert(sceneIndex,value){const scenes=parse(),s=scenes[sceneIndex];if(!s)return;const rows=ta.value.split('\n'),label=s.sourceText||'[pause]';rows[s.lineIndex]=`${label} | ${s.seconds}${value?' | inv':''}`;ta.value=rows.join('\n');idx=sceneIndex;refresh(false);selectScene(sceneIndex)}
function renderList(){const scenes=parse();count.textContent=`${scenes.length} scene${scenes.length===1?'':'s'}`;list.innerHTML='';let total=0;scenes.forEach((s,i)=>{const row=document.createElement('div');row.className='scene-row'+(i===idx?' active':'');row.tabIndex=0;row.innerHTML=`<span>${String(i+1).padStart(2,'0')}</span><b>${s.text||'PAUSE'}</b><em>${s.seconds.toFixed(2)}s</em><button class="scene-invert${s.invert?' on':''}" type="button">INV</button>`;row.addEventListener('click',e=>{if(!e.target.closest('.scene-invert'))selectScene(i)});row.querySelector('.scene-invert').onclick=e=>{e.stopPropagation();setSceneInvert(i,!s.invert)};list.append(row);total+=s.seconds});return{scenes,total}}
function refresh(reset=true){stop();const {scenes,total}=renderList();if(reset)idx=0;if(idx>=scenes.length)idx=Math.max(0,scenes.length-1);const s=scenes[idx];draw(s?.text||'',!!s?.invert,1);status.textContent=s?`Scene ${idx+1} · ${s.text||'Pause'}${s.invert?' · INVERT':''}`:'No scenes';timeStatus.textContent=`Total ${total.toFixed(2)}s`;syncSelected()}
ta.addEventListener('input',()=>refresh(true));[style,size,animation].forEach(e=>e.addEventListener('input',()=>refresh(false)));inv.addEventListener('change',()=>setSceneInvert(idx,inv.checked));
function stop(){if(raf)cancelAnimationFrame(raf);raf=0}
function play(){stop();const {scenes,total}=renderList();if(!scenes.length)return;const start=performance.now();function tick(now){let t=(now-start)/1000;if(t>=total){const s=scenes[scenes.length-1];idx=scenes.length-1;draw(s.text,s.invert,1);timeStatus.textContent=`Finished · ${total.toFixed(2)}s`;return}let acc=0,i=0;for(;i<scenes.length;i++){if(t<acc+scenes[i].seconds)break;acc+=scenes[i].seconds}const s=scenes[Math.min(i,scenes.length-1)],local=t-acc,p=s.seconds?local/s.seconds:1;idx=i;draw(s.text,s.invert,p);status.textContent=`Scene ${i+1} · ${s.text||'Pause'}${s.invert?' · INVERT':''}`;timeStatus.textContent=`${t.toFixed(2)} / ${total.toFixed(2)}s`;syncSelected();raf=requestAnimationFrame(tick)}raf=requestAnimationFrame(tick)}
$('#playLyrics').onclick=play;
$('#exportLyrics').onclick=()=>{const {scenes}=renderList();let out='#pragma once\n#include <Arduino.h>\n\nstruct LyricScene { const char* text; uint32_t duration; bool invert; };\n';out+=`// style=${style.value}, animation=${animation.value}\nconst LyricScene lyricScenes[] PROGMEM = {\n`;for(const s of scenes){const safe=s.text.replace(/\\/g,'\\\\').replace(/"/g,'\\"');out+=`  {"${safe}", ${Math.round(s.seconds*1000)}, ${s.invert?'true':'false'}},\n`}out+='};\nconst uint16_t LYRIC_SCENE_COUNT = sizeof(lyricScenes) / sizeof(lyricScenes[0]);\n';const blob=new Blob([out],{type:'text/plain'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download='Lyrics.h';a.click();setTimeout(()=>URL.revokeObjectURL(u),500)};
const buildBtn=$('#buildBin'),buildBox=$('#buildStatus'),buildTitle=$('#buildStatusTitle'),buildText=$('#buildStatusText'),downloadBin=$('#downloadBuiltBin');
const wait=ms=>new Promise(r=>setTimeout(r,ms));
async function pollBuild(commit){for(let attempt=0;attempt<60;attempt++){await wait(attempt?4000:2500);try{const r=await fetch(`/builds.json?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)continue;const builds=await r.json();const found=builds.find(b=>b.commit===commit);if(found?.status==='ready'&&found.file)return found;if(found?.status==='failed')throw new Error('Compiler gagal. Cek BIN Builds.');buildText.textContent=`Compiler masih bekerja... ${Math.min(99,10+attempt*2)}%`}catch(e){if(String(e.message).includes('Compiler gagal'))throw e}}throw new Error('Build belum selesai setelah 4 menit. Cek halaman BIN Builds.')}
async function buildBin(){const scenes=parse();if(!scenes.length)return;buildBtn.disabled=true;downloadBin.classList.add('hidden');buildBox.classList.remove('hidden');buildTitle.textContent='Mengirim scene...';buildText.textContent='Membuat konfigurasi firmware.';try{const payload={style:style.value,animation:animation.value,size:+size.value,scenes:scenes.map(s=>({text:s.text,duration:Math.round(s.seconds*1000),invert:s.invert}))};const r=await fetch('/api/build-lyrics',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await r.json().catch(()=>({}));if(!r.ok){if(data.code==='TOKEN_MISSING')throw new Error('Build service belum aktif. GITHUB_BUILD_TOKEN belum dipasang di Vercel.');throw new Error(data.error||'Gagal memulai build')}buildTitle.textContent=`Build ${data.short} dimulai`;buildText.textContent=`Render ${animation.options[animation.selectedIndex].text} + compile OTA .bin.`;const result=await pollBuild(data.commit);buildTitle.textContent='OTA .BIN siap';buildText.textContent=`${Math.round(result.size/1024)} KB · ${result.id}`;downloadBin.href='/'+result.file;downloadBin.download='';downloadBin.classList.remove('hidden')}catch(e){buildTitle.textContent='Build belum berhasil';buildText.textContent=e.message||String(e)}finally{buildBtn.disabled=false}}
buildBtn.onclick=buildBin;refresh();